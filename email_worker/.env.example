# IMAP settings (for mailhog-imap container)
IMAP_HOST=mailhog
IMAP_PORT=1143
IMAP_USER=testuser
IMAP_PASSWORD=testpass

IMAP_POLL_INTERVAL_SECONDS=30

# SMTP settings (for mailhog container)
SMTP_HOST=mailhog
SMTP_PORT=1025
SMTP_USER=
SMTP_PASSWORD=

EMAIL_FROM_ADDRESS=helpdesk@sps.com
EMAIL_FROM_NAME=SPS Helpdesk

# Backend API (Docker service name)
BACKEND_API_URL=http://backend:8000

# AI Classification Service (Docker service name)
AI_SERVICE_URL=http://ai_service:8001

# Portal URL (for links in email notifications)
PORTAL_URL=http://localhost:5173